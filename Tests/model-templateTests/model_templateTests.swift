import XCTest
@testable import model_template

class model_templateTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssertEqual(model_template().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
